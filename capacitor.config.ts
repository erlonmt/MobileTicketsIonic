import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.edu.uninassau.mobileticketsionic',
  appName: 'MobileTicketsIonic',
  webDir: 'www',
};

export default config;
